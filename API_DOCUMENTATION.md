# API Endpoints та Routes - Документація

Цей документ описує всі доступні маршрути (routes) та API endpoints веб-застосунку.

## 📋 Зміст

- [Маршрути (Routes)](#маршрути-routes)
- [API Endpoints](#api-endpoints)
- [JavaScript API](#javascript-api)
- [Структура проекту](#структура-проекту)

---

## 🛣️ Маршрути (Routes)

Всі маршрути обробляються через головний роутер `index.php`. Роутер аналізує URL та перенаправляє на відповідну сторінку.

### Таблиця маршрутів

| Метод | Маршрут | Опис | Файл | Заголовок сторінки |
|-------|---------|------|------|-------------------|
| `GET` | `/` | Головна сторінка сайту | `pages/home.php` | `Wikipedia - Вільна енциклопедія` |
| `GET` | `/home` | Головна сторінка сайту (альтернативний маршрут) | `pages/home.php` | `Wikipedia - Вільна енциклопедія` |
| `GET` | `/login` | Сторінка авторизації та реєстрації | `pages/login.php` | `Авторизація - Wikipedia` |
| `GET` | `/api` | Демонстрація роботи з API (Random User API) | `pages/api.php` | `API Demo - Wikipedia` |
| `GET` | `/game` | 2D гра "Гонка з перешкодами" | `pages/game.php` | `Гонка - Wikipedia` |
| `GET` | `/*` | Сторінка помилки 404 (будь-який невідомий маршрут) | `pages/404.php` | `404 - Сторінку не знайдено` |

### Детальний опис маршрутів

#### 1. Головна сторінка (`/` або `/home`)

**Опис:** Головна сторінка сайту з навігацією, мовами та демонстрацією JavaScript функцій.

**Особливості:**
- Автоматичне завантаження зображень через 5 секунд після завантаження сторінки
- Використання `setTimeout`, `createDocumentFragment`, `forEach`
- Навігаційні кнопки: Увійти, API, Гра

**JavaScript функції:**
- `loadImages()` - завантажує зображення з масиву URL

**Файли:**
- `pages/home.php`
- `scripts/script.js`

---

#### 2. Сторінка авторизації (`/login`)

**Опис:** Сторінка з формами авторизації та реєстрації, валідацією полів та порівнянням текстів.

**Функціональність:**
- Валідація полів: Login, Email, Phone
- Використання регулярних виразів (`match`, `search`)
- Очищення поля Login за допомогою `replace`
- Порівняння текстів з використанням `Set`
- Відображення спільних слів між двома текстами

**Валідація:**
- **Login:** 3-20 символів, латиниця, цифри, підкреслення
- **Email:** стандартний формат email
- **Phone:** формат `+380XXXXXXXXX` або `0XXXXXXXXX`

**JavaScript функції:**
- `validateLogin(value)` - валідація логіну
- `validateEmail(value)` - валідація email
- `validatePhone(value)` - валідація телефону
- `loadImages()` - завантаження зображень
- `findCommonWords(text1, text2)` - пошук спільних слів

**Файли:**
- `pages/login.php`
- `scripts/validation.js`
- `scripts/set-comparison.js`

---

#### 3. API Demo (`/api`)

**Опис:** Демонстрація роботи з зовнішнім API (Random User API) з використанням асинхронних функцій.

**API Endpoint:**
- **URL:** `https://randomuser.me/api/?results={count}&nat=us,gb,ua`
- **Метод:** `GET`
- **Параметри:**
  - `results` (number) - кількість користувачів (за замовчуванням: 6)
  - `nat` (string) - національності (us, gb, ua)

**Функціональність:**
- Асинхронне завантаження даних через `fetch`
- Використання `async/await`
- Обробка помилок через `try/catch`
- Відображення карток користувачів з фото, ім'ям, email, телефоном та локацією

**JavaScript функції:**
- `fetchRandomUsers(count)` - асинхронна функція для отримання користувачів
- `displayUsers(users)` - відображення користувачів на сторінці

**Приклад використання:**
```javascript
const data = await fetchRandomUsers(6);
displayUsers(data.results);
```

**Файли:**
- `pages/api.php`
- `scripts/api-fetch.js`

---

#### 4. Гра "Гонка з перешкодами" (`/game`)

**Опис:** 2D гра на Canvas з системою очок, перешкодами та зростаючою складністю.

**Функціональність:**
- Управління машинкою стрілками ← →
- Система очок (+10 за кожну пропущену перешкоду)
- Зростаюча швидкість з часом
- Збереження рекорду в `localStorage`
- Екран Game Over з можливістю перезапуску

**Управління:**
- `←` (ArrowLeft) - рух вліво
- `→` (ArrowRight) - рух вправо
- `Пробіл` - початок гри / перезапуск

**Game Objects:**
- `car` - об'єкт машинки гравця
- `obstacles[]` - масив перешкод
- `score` - поточний рахунок
- `speed` - поточний рівень швидкості

**JavaScript функції:**
- `startGame()` - початок нової гри
- `gameLoop()` - головний цикл гри
- `drawRoad()` - малювання дороги
- `drawCar()` - малювання машинки
- `spawnObstacle()` - створення нової перешкоди
- `checkCollision()` - перевірка зіткнень
- `endGame()` - завершення гри
- `restartGame()` - перезапуск гри

**Файли:**
- `pages/game.php`
- `scripts/game.js`

---

#### 5. Сторінка 404 (`/*`)

**Опис:** Сторінка помилки для невідомих маршрутів.

**Особливості:**
- Автоматичне відображення для будь-якого невідомого маршруту
- HTTP статус код: `404`
- Навігаційні кнопки для повернення на головну

**Файли:**
- `pages/404.php`

---

## 🔌 API Endpoints

### Зовнішні API

#### Random User API

**Base URL:** `https://randomuser.me/api/`

**Endpoint:** `/api/?results={count}&nat={nationalities}`

**Метод:** `GET`

**Параметри:**
- `results` (integer, optional) - кількість користувачів (1-5000)
- `nat` (string, optional) - національності через кому (us, gb, ua, тощо)

**Приклад запиту:**
```
GET https://randomuser.me/api/?results=6&nat=us,gb,ua
```

**Приклад відповіді:**
```json
{
  "results": [
    {
      "name": {
        "title": "Mr",
        "first": "John",
        "last": "Doe"
      },
      "email": "john.doe@example.com",
      "phone": "+1-234-567-8900",
      "picture": {
        "large": "https://randomuser.me/api/portraits/men/1.jpg"
      },
      "location": {
        "city": "New York",
        "country": "United States"
      }
    }
  ]
}
```

**Документація:** [https://randomuser.me/documentation](https://randomuser.me/documentation)

---

## 📜 JavaScript API

### Глобальні функції та об'єкти

#### 1. Валідація форм (`scripts/validation.js`)

**Регулярні вирази:**
- `loginRegex` - `/^[a-zA-Z0-9_]{3,20}$/`
- `emailRegex` - `/^[^\s@]+@[^\s@]+\.[^\s@]+$/`
- `phoneRegex` - `/^(\+38)?0\d{9}$/`

**Функції:**
- `validateLogin(value)` - валідація логіну
- `validateEmail(value)` - валідація email
- `validatePhone(value)` - валідація телефону

#### 2. Порівняння текстів (`scripts/set-comparison.js`)

**Функції:**
- `getWordsSet(text)` - створення Set зі слів тексту
- `findCommonWords(text1, text2)` - пошук спільних слів між двома текстами

#### 3. API Fetch (`scripts/api-fetch.js`)

**Функції:**
- `fetchRandomUsers(count)` - асинхронне отримання користувачів
- `displayUsers(users)` - відображення користувачів

#### 4. Гра (`scripts/game.js`)

**Глобальні змінні:**
- `canvas` - Canvas елемент
- `ctx` - 2D контекст Canvas
- `gameRunning` - статус гри
- `score` - поточний рахунок
- `car` - об'єкт машинки
- `obstacles` - масив перешкод

**Функції:**
- `startGame()` - початок гри
- `gameLoop()` - головний цикл
- `endGame()` - завершення гри
- `restartGame()` - перезапуск

---

## 📁 Структура проекту

```
project-1/
├── index.php                 # Головний роутер
├── .htaccess                 # Правила перенаправлення Apache
├── README.md                  # Основна документація
├── API_DOCUMENTATION.md       # Документація API та Routes
├── pages/
│   ├── home.php              # Головна сторінка
│   ├── login.php             # Сторінка авторизації
│   ├── api.php               # API Demo
│   ├── game.php              # Гра
│   └── 404.php               # Сторінка помилки
├── scripts/
│   ├── script.js             # Скрипт для головної сторінки
│   ├── validation.js         # Валідація форм
│   ├── set-comparison.js     # Порівняння текстів
│   ├── api-fetch.js          # Робота з API
│   └── game.js               # Логіка гри
├── styles/
│   └── style.css             # Стилі сайту
└── img/                      # Зображення та іконки
```

---

## 🚀 Як використовувати

### Локальний запуск

```bash
# Запуск PHP вбудованого сервера
php -S localhost:8000

# Відкрити в браузері
http://localhost:8000
```

### Доступні маршрути

- `http://localhost:8000/` - Головна сторінка
- `http://localhost:8000/home` - Головна сторінка (альтернативний)
- `http://localhost:8000/login` - Авторизація
- `http://localhost:8000/api` - API Demo
- `http://localhost:8000/game` - Гра
- `http://localhost:8000/any-other-route` - 404 сторінка

---

## 📝 Примітки

1. **Роутинг:** Всі маршрути обробляються через `index.php` завдяки `.htaccess` правилам
2. **404 обробка:** Будь-який невідомий маршрут автоматично перенаправляється на сторінку 404
3. **Динамічні заголовки:** Заголовок сторінки (`<title>`) встановлюється динамічно в залежності від маршруту
4. **Навігація:** На всіх сторінках є навігаційні кнопки для швидкого переходу між сторінками

---

## 🔗 Корисні посилання

- [Random User API Documentation](https://randomuser.me/documentation)
- [MDN Web Docs - Fetch API](https://developer.mozilla.org/en-US/docs/Web/API/Fetch_API)
- [MDN Web Docs - Async/Await](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Statements/async_function)
- [MDN Web Docs - Canvas API](https://developer.mozilla.org/en-US/docs/Web/API/Canvas_API)

---

**Версія документації:** 1.0  
**Останнє оновлення:** 2024  
**Автор:** Проєкт для курсу веб-технологій та PHP

